version: '3.8'

services:
  # web:
  #   build: .
  #   env_file:
  #     - app.env
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - .:/usr/src/app
  #   command: air ./cmd/main.go -b 0.0.0.0
  # db:
  #   image: postgres:latest
  #   restart: always
  #   environment:
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=StrongPw01
  #     - POSTGRES_DB=postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - .:/usr/src/app
  #     - ./src/migrations/up.sql:/docker-entrypoint-initdb.d/create_tables.sql
  # api: 
  #   container_name: todo-awesome
  #   build: .
  #   image: todo-awesome
  #   depends_on:
  #   - db
  #   ports:
  #     - 8080:8080
  #   environment:
  #     DB_HOST: db
  #     DB_PORT: 5432
  #     DB_USER: postgres
  #     DB_PASSWORD: StrongPw01
  #     DB_NAME: postgres
  #   links:
  #     - db
  #   volumes:
  #     - './:/src'
  db: 
    image : postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports: 
      - '5432:5432'
    volumes:
      # - db:/var/lib/postgresql/data
      - ./src/migrations/up.sql:/docker-entrypoint-initdb.d/up.sql
  api: 
    container_name: todo-awesome
    build: .
    image: todo-awesome
    depends_on:
    - db
    ports:
      - 8080:8080
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: StrongPw01
      DB_NAME: postgres
    links:
      - db
    volumes:
      - './:/src'
volumes: 
  db: 
    driver: local